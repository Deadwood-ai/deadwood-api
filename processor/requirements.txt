pydantic-settings
pydantic_geojson
pydantic_partial
python-dotenv
python-multipart
supabase
gotrue
prometheus-client
rasterio==1.5.1
rio-cogeo
fire
pillow>=10.0.0
paramiko
requests
httpx
docker>=6.1.0
tqdm
geopandas
utm
scikit-learn
matplotlib
safetensors
segmentation_models_pytorch
# Keep this pair aligned with the model checkpoints and open-clip stack. The
# Dockerfile selects CUDA or CPU wheels without owning a second set of pins.
torch==2.6.0
torchvision==0.21.0
open-clip-torch>=2.24.0
opencv-python
xarray>=2023.1.0
zarr>=2.13.0
# Rasterio 1.5 still triggers NumPy 2.5's deprecated ndarray shape mutation
# path during normal reads. Keep the service below 2.5 until Rasterio removes it.
numpy>=2,<2.5
# Visvalingam-Whyatt polygon simplification (deadwood_treecover_combined_v2 postprocessing)
simplification>=1.0.0,<2
# Pin to 4.x: transformers 5.0 renamed SegFormer state-dict keys
# (encoder.block.* -> stages.*, decode_head.linear_c -> linear_projections),
# which breaks load_state_dict(strict=True) for the AOI and combined-model
# checkpoints. See processor/src/aoi_segmentation_v1 and deadwood_treecover_combined_v2.
transformers<5
